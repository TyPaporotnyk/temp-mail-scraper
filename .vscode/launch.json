{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run server",
      "type": "debugpy",
      "request": "launch",
      "program": "${workspaceFolder}/bin/run.py",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceFolder}"
      }
    }
  ]
}
